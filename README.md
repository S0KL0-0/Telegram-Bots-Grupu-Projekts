Dependencies:
<code>npm install</code>

Make <code>.env</code> containing:

<code>API_PORT=10000</code>

<code>BOT_TOKEN=telegram_bot_token</code>

BOT_TOKEN is optional. If omitted, the bot is disabled.

Run <code>npm start</code>